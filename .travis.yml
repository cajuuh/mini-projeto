language: csharp
solution: mini-projeto.sln

sudo: required

install:
  -./Scripts/install.sh

script:    # the following commands are just examples, use whatever your build process requires
  - ./Scripts/build.sh